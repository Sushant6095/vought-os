# 40 · Pages

Per-page UX specs and implementation notes. One note per URL.

When implementing a page, read the existing note first. When finishing, update it.

Use [[Page Template]] for new entries.
