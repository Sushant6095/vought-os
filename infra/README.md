# infra/

Infrastructure as code, deploy targets, docker-compose for local dev.

- `docker-compose.yml` — Postgres + Redis for local dev
- `deploy/` — deploy manifests per environment

See `vault/30 · Architecture/System Diagram.md` for the production deployment plan.
